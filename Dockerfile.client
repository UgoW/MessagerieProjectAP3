FROM fedora:38

RUN dnf update -y && \
    dnf install -y gcc make openssl openssl-devel && \
    dnf clean all && \
    rm -rf /var/cache/dnf

WORKDIR /app

COPY . .

RUN gcc server.c -o server -lssl -lcrypto && gcc client.c -o client -lssl -lcrypto

CMD ["/bin/bash"]